#include<bits/stdc++.h>
using namespace std;

int main(){
    // string text = "ACBAACAADAABAABA";
    // string pattern = "AABA";
    // int lenP;
    // lenP = pattern.length();
    // int index;
    // for(int i=0; i<text.length()-lenP; i++){
    //     int j = 0;
    //     for(j; j<lenP; j++){
    //         if(text[i+j] == pattern[j]){
    //             if(j == lenP){
    //                 index = i;
    //             }
    //         }
    //         else break;
    //     }
    // }
    // cout << index << endl;

    string text = "ACBAACAADAABAABA";
    string pattern = "AABA";
    int lenP, lenT;
    lenP = pattern.length();
    lenT = text.length();
    for(int i=0; i<text.length()-lenP; i++){
        int j=0;
        //while((j<lenP)&&)
    }
}