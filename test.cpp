#include<bits/stdc++.h>
using namespace std;

void game0(int x, int *score, int n){
    int table[n][x];
    memset(table, 0, sizeof(table));
    for(int i=1; i<n; i++){
        for(int j=1; j<x; j++){
            
        }
    }
}

int main(){
    int n, p;
    cin >> n >> p;
    int x, y, z;
    cin >> x >> y >> z;
    int* score = new int[n + 1];
    int* color = new int[n + 1];
    for(int i=0; i<n; i++){
        cin >> score[i];
    }
    for(int i=0; i<n; i++){
        cin >> color[i];
    }
    if(p==0){

    }
    if(p==1){

    }
    if(p==2){

    }
}